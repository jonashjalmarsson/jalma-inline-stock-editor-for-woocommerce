.git/
.gitignore
.vscode/
.claude/
.DS_Store
.distignore
RELEASE-CHECKLIST.md
bin/
.dist/
dist/
changelog.md
readme.md
trunk/
# wp.org-specific assets (banner/icon/screenshots) live in assets/ and are
# synced separately to the SVN /assets/ folder on wp.org — they must NOT be
# bundled in the plugin zip. Runtime JS/CSS lives in admin/ which does ship.
assets/
